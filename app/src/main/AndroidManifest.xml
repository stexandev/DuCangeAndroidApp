<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.stexan.ducangeandroidapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Search">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity
            android:name=".CheckDatabaseActivity"
            android:label="@string/title_activity_check_database"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.Dialog">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="de.stexan.ducangeandroidapp.MainActivity" />
        </activity>

        <service android:name=".DatabaseFileService"
            android:exported="false">
            <intent-filter>
                <action android:name="@string/copy_action_name" />
                <action android:name="@string/dl_action_name" />
                <action android:name="@string/unzip_action_name" />
            </intent-filter>
        </service>
        <!--
        <receiver android:name=".DatabaseFileServiceReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="@string/copy_complete_action_name" />
                <action android:name="@string/unzip_complete_action_name" />
                <action android:name="android.intent.action.DOWNLOAD_COMPLETE"/>
            </intent-filter>
        </receiver>
        -->
        <meta-data android:name="android.webkit.WebView.MetricsOptOut"
            android:value="true" />
    </application>

</manifest>